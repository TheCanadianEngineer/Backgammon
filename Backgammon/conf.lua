function love.conf(t)
    t.window.fullscreen = true      -- Enable fullscreen mode
    t.window.fullscreentype = "desktop"  -- Fullscreen type can be "desktop" or "exclusive"
    t.console = true
end
